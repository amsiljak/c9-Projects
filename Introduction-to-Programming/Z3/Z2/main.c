#include <stdio.h>
double parallel_max(float A[], float B[]) {
	if (p1-p2==1) return *p1;
	if(n==1) return A{[0];
	return max(A[n-1], parallel_max(A,))
}

int main() {
	printf("Zadaća 3, Zadatak 2");
	return 0;
}
